# Diagnostic exclusion (2026-09-20): api/hermes/status.ts and middleware.ts
# have real, longstanding TypeScript errors (missing @types/node, missing
# next/server types) that Vercel's zero-config Function detection tries to
# compile on every deploy regardless of the root "no build needed" script.
# Testing whether this is why static files (e.g. renta-natural.html) stop
# updating on some deploys -- see openspec/changes/wizard-iva-ecom-express-
# diagnostic/ for the investigation. NOT a decision to retire these files --
# flagged separately for the founder to decide their fate.
api/hermes/status.ts
middleware.ts

# Backend (deploys to Railway, NOT Vercel) — 81 MB incl. .venv
apps/
venv_contexia/
**/.venv/
**/venv/
**/__pycache__/
**/.pytest_cache/

# Frontend builds / dev
# _next/
node_modules/
**/node_modules/
.next/
**/.next/
build/
dist/
.vercel/

# IDE & version control
.claude/
.cursor/
.git/
.github/
.vscode/
.idea/

# Docs & specs (not needed in deployed app)
specs/
docs/
ai-specs/
openspec/
.antigravity/

# Monorepo projects not served by this Vercel deploy
contexia-app/
contexia-wizard/
frontend/
backend/
copiloto-contratos-eafit/

# Logs & temps
*.log
*.tmp
.DS_Store
.env.local
.env.*.local
