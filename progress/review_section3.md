# Review — task section3 (approval-queue-tenant-scoping)

**Verdict:** APPROVED

## Checkpoints
- C1 (resolution_agent_service.py threads existing tenant_id, not a no-op): [x] — `git show 264243a` diff confirms both call sites (`generate_draft` ~line 110, `generate_draft_with_retry` ~line 162) add `tenant_id=tenant_id`, where `tenant_id` is an existing function parameter already used earlier in each function body (not a stray/undefined name, not a typo).
- C2 (social_ops_service.py explicit resolution + None-safe skip): [x] — `resolve_cliente_cero_tenant_id(get_supabase())` called at the `draft_lead_reply` call site (services/social_ops_service.py:830), `if not tenant_id: log.warning(...)` else enqueue; draft is still `return deepcopy(draft)` regardless. `get_supabase()` (anon client) is correct — `core/tenant_context.py:22`'s `resolve_cliente_cero_tenant_id` only does a read of `tenants.is_cliente_cero`, same anon-client pattern used elsewhere (design.md's resolution ladder); `get_service_supabase()` is reserved for governance ops per its own docstring (core/supabase_client.py:15-23), not needed here.
- C3 (sell_machine_service.py RuntimeError deviation): [x] — justified, not scope creep. `create_campaign_package`'s existing docstring states "Raises RuntimeError if enqueueing fails" (services/sell_machine_service.py:102) and it already has a "raise on failure" path for the DB-failure case (`if not success: raise RuntimeError(...)`, line 131-132). Treating unresolved-tenant as an enqueue failure keeps the function inside its documented raise-or-succeed contract; `presentation/sell_machine_endpoints.py:97` doesn't catch RuntimeError today either way, so this is not a new failure mode for callers, just a new trigger for an existing one. Reasonable judgment call, correctly distinguished from `social_ops_service.py`'s genuine skip (that function already tolerates best-effort side effects).
- C4 (approval_queue_endpoints.py:122 intentionally left broken): [x] — verified: `enqueue_for_approval` (presentation/approval_queue_endpoints.py, ~line 119) still calls `enqueue_draft(...)` without `tenant_id`, which will now raise `tenant_id is required` per Section 2's contract. Correctly scoped to Section 4; not touched in this commit (`git diff 264243a~1 264243a -- presentation/approval_queue_endpoints.py` is empty).
- C5 (social_ops_endpoints.py approve/reject are unrelated in-memory methods): [x] — confirmed `presentation/social_ops_endpoints.py:244,254` call `SocialOpsService.approve_draft`/`reject_draft` (services/social_ops_service.py:929,947), which are synchronous in-memory dict methods (`self.command_drafts`, `self.reply_drafts`, etc.) unrelated to `ApprovalQueueService.approve_draft`/`reject_draft`. Correctly out of scope.
- C6 (targeted test run green): [x] — `pytest tests/test_tenant_stamping.py tests/test_approval_queue_service_scoping.py tests/test_tenant_scope_resolution.py tests/test_social_ops_endpoints.py tests/test_sell_machine_service.py tests/test_operator_task_service.py -q` → 47 passed, matches implementer's report.
- C7 (pre-existing failures unrelated to Section 3): [x] — ran full suite (`--ignore` the 3 pre-agreed import-path-broken files): 591 passed, 40 failed, 109 skipped, 13 errors — same counts as reported. Failure names are `test_shadow_gl_siigo_csv.py`, `test_approval_rules_stage*_acceptance` (doc/migration-existence checks for other OpenSpec changes), `test_financials_aggregation.py`/`test_financials_endpoint_tenant_scoping.py` errors, `test_centinela_alerts_get.py`, `test_wizard_auditoria_sombra.py` — all traced to a `starlette.testclient`/`httpx` version mismatch (`Client.__init__() got an unexpected keyword argument 'app'`) or Siigo CSV/doc-existence assertions unrelated to approval_queue/enqueue_draft/social_ops/sell_machine. `git diff --stat` for this commit touches only 5 files (3 services + 2 test files), none of which are imported by the failing test modules.
- C8 (scope discipline — endpoints.py and migration 0033 untouched): [x] — confirmed no diff on `presentation/approval_queue_endpoints.py`; `ls apps/backend/migrations | grep 0033` returns nothing (migration not created, correctly deferred to Section 8).
- Docs-sync: N/A — no container/dependency change; internal service wiring only, no ARCHITECTURE.md update required.

## Notes
No required changes. The RuntimeError deviation (C3) was scrutinized closely as the highest-risk judgment call and holds up against the actual function contract and its only caller.
