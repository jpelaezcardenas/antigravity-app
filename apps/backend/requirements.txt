fastapi==0.104.1
uvicorn[standard]==0.24.0
pydantic==2.5.0
pydantic-settings==2.1.0
sqlalchemy==2.0.23
supabase==2.0.3
psycopg2-binary==2.9.9
python-jose[cryptography]==3.3.0
passlib[bcrypt]==1.7.4
bcrypt<4.1  # passlib 1.7.4 reads bcrypt.__about__.__version__, removed in bcrypt>=4.1 — pins the
            # compatible range (found live during bunker-pwa-auth-enforcement's Stage 11 prep;
            # unpinned bcrypt would silently break every password hash/verify call)
python-dotenv==1.0.0
httpx==0.24.1
python-multipart==0.0.6
pytest==7.4.3
pytest-asyncio==0.21.1
email-validator>=2.0.0
requests
slowapi==0.1.9
openai>=1.0.0
groq>=0.11.0
google-generativeai>=0.8.0  # kb_seeding_service.py's Gemini embedding fallback — declared but
                            # never installed in requirements (found live 2026-08-11,
                            # taty-whatsapp-renta-sales-capability): the fallback was silently
                            # dead code in every environment, including production, whenever the
                            # primary OpenAI embedding path was unavailable.
websockets>=12.0
prometheus-client==0.20.0

# Force rebuild - Phase 5 deployment 1782229689
