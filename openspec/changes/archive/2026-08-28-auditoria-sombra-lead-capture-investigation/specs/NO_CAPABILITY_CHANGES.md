# No spec-level changes

This change is a read-only investigation (see `proposal.md` and `design.md`). It introduces no new
capability and modifies no existing capability's requirements, so there are no delta specs to write
here. The `openspec/specs/` tree is unchanged by this change.
