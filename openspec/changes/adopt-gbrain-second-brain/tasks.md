## 1. Raw Loop Foundation (Layer 0) — two-repo split

- [x] 1.0 Create the separate `contexia-brain` sibling repo (git init) — corrected mid-implementation: brain content must never live inside `antigravity-app` (its `main` auto-deploys to Vercel/Railway on every push; GBrain's autonomous maintenance cycle auto-commits to the brain repo on a schedule, per GBrain's own `docs/guides/repo-architecture.md` two-repo rule)
- [x] 1.1 Create `contexia-brain/raw/` folder with a README stating its purpose (unstructured inbox, no required format)
- [x] 1.2 Verified in practice rather than a separate written checklist: `test-entity-capture.md` (task 4.3) was dropped into `contexia-brain/raw/` with an arbitrary name, no frontmatter, no naming convention — accepted and processed without issue.
- [x] 1.3 Extend `antigravity-app`'s root `CLAUDE.md`/`AGENTS.md` with librarian instructions: read `contexia-brain/raw/`, propose updates to the relevant target in `contexia-brain` or a canon doc, log uncertainty instead of guessing, never delete/overwrite blindly — explicitly mark this manual loop as the **interim/fallback** path, superseded by GBrain's autonomous maintenance cycle once live, and explicitly forbid creating `raw/`/brain/`prompts/`/`projects/` folders inside `antigravity-app`
- [x] 1.4 Confirm the librarian instructions reference `ai-specs/skills/`, `openspec/changes/`, and `contexia-brain/` correctly — review diff to ensure no parallel folder was introduced inside `antigravity-app`
- [x] 1.5 Create the Contexia-adapted MECE directory structure in `contexia-brain` (`people/`, `companies/`, `deals/`, `meetings/`, `concepts/`, `ideas/`, `media/`, `sources/`, `archive/`), each with a `README.md` resolver, plus a top-level `RESOLVER.md` decision tree and `templates/compiled-page.md` (compiled-truth + timeline); document the deliberate exclusions (`projects/`, `prompts/`, `civic/`, `household/`, `personal/`, `hiring/`, `diligence/`) and why
- [x] 1.6 Write `scripts/harvest_stage11_reports.py` in `antigravity-app` (reads Stage 11 reports from `antigravity-app`, writes references into `contexia-brain/raw/` — cross-repo by design, verified end-to-end with a real run: 45 reports harvested)
- [x] 1.7 Add de-duplication ledger (`contexia-brain/raw/_harvested.json`, content-hash keyed) so an already-harvested report is not re-harvested on subsequent runs — verified via a second real run (0 harvested, 45 skipped)
- [x] 1.8 Same real evidence as task 4.3: `test-entity-capture.md` dropped into `raw/`, processed by the **primary path** (GBrain's autonomous maintenance cycle, since it's live — per the spec's own precedence rule, manual librarian loop is only the fallback) — correctly synced, embedded, and made genuinely searchable. Sane, non-guessed placement (the raw file, not a fabricated compiled page) since no confident compiled-page target existed yet — consistent with "log uncertainty rather than guessing."
- [x] 1.9 Pushed to `github.com/jpelaezcardenas/contexia-brain` (private repo, created via `gh repo create --source=. --remote=origin`, founder confirmed `gh auth login` completed first)

## 2. GBrain Installation & Isolation

- [x] 2.0 Install Bun natively (`irm bun.sh/install.ps1 | iex` on Windows — the documented path; NOT `bun install -g github:...`, which is a known-broken path per GBrain's own issue #218, and NOT via npm, which creates PATH/shim conflicts with GBrain's compiled binary launcher)
- [x] 2.0.1 Clone GBrain from `github.com/jpelaezcardenas/garrytan-gbrain` as a local sidecar (verified this is the real tool — confirmed distinct from the unrelated `gbrain` npm package, a different "GPU ML library" by another author; do not install that)
- [x] 2.0.2 `bun install && bun link` in the cloned GBrain repo; verify `gbrain --version` resolves on PATH
- [x] 2.1 Provision a dedicated `gbrain` schema in Contexia's active Supabase project (`kpynymwghfwshvcvevxq`, confirmed via table inspection to be the real production DB, dashboard-labeled "contexia-content-os" but holding live `erp_journal_entries`/`centinela_alerts`/`approval_queue` data; the other candidate project literally named "Contexia" was confirmed stale/empty with 8 RLS-disabled tables — flagged to founder separately, not remediated without explicit instruction). Founder confirmed target project explicitly before the migration ran. `CREATE SCHEMA gbrain` + `CREATE EXTENSION vector` applied; verified `public.knowledge_chunks` unchanged
- [x] 2.2 `gbrain init` ran locally against PGLite (validated: 58 pages, 100% embedded, hybrid search proven). `gbrain migrate --to supabase` then run pointed at the `gbrain` schema (via `GBRAIN_DATABASE_URL` with `?options=-c search_path=gbrain,public` against the Session pooler — Direct connection times out from this network, IPv6-only per Supabase's own dashboard note). Migration verified: 58/58 pages, 100% embedding coverage, schema version 16, matches source exactly. Original PGLite brain preserved as backup at `~/.gbrain/brain.pglite`.
- [x] 2.3 Confirmed via Supabase MCP `list_tables`: `gbrain.*` (pages=58, content_chunks=59, links=0, + 8 more GBrain-internal tables) has zero name overlap with `public.*`; `public.knowledge_chunks` unchanged (0 rows, RLS enabled, same as before migration). **Finding closed**: 10 of GBrain's own tables (`access_tokens`, `mcp_request_log`, `minion_inbox`, `minion_attachments`, `subagent_messages`, `subagent_tool_executions`, `subagent_rate_leases`, `gbrain_cycle_locks`, `budget_ledger`, `budget_reservations`) were created without RLS — internal plumbing, not user data, but still anon-key-reachable. Founder approved; RLS enabled on all 10 via migration `enable_rls_gbrain_internal_tables`. Verified GBrain still fully functional afterward (`gbrain stats`: 58 pages/59 chunks/59 embedded, unchanged) — the `postgres` role GBrain connects with bypasses RLS regardless, so this was zero-risk to apply.
- [x] 2.4 `OPENAI_API_KEY` retrieved from Bitwarden (item "Open Ai Hermes Gbrain") and written to `gbrain-contexia/.env` (gitignored, verified via `git check-ignore`) — separate key from the FastAPI backend's own. `ANTHROPIC_API_KEY` deliberately **not configured**: the Bitwarden item labeled for it actually held an OpenAI-format key (`sk-proj-...`, not `sk-ant-api03-...`); founder chose to skip Claude-Haiku multi-query expansion for now rather than use a mismatched value. GBrain runs correctly without it (per its own docs — vector+keyword search still fully functional, only query expansion is skipped). Revisit when a real Anthropic key is available.
- [x] 2.5 `gbrain import contexia-brain/ --no-embed` (58 pages, 59 chunks, 0 errors) then `gbrain embed --stale`. Took 3 attempts to get a working key: (1) original Bitwarden key was invalid — 401 on every call; (2) replacement key authenticated but its account had zero billing/quota — 429 on every call from the first request; (3) an `sk-admin-...` key with confirmed balance worked — verified with a direct `/v1/embeddings` curl probe before the full run, then the full run: 58/58 pages, 59/59 chunks, **0 errors**. `gbrain doctor` confirms `embeddings: 100% coverage, 0 missing`.
- [x] 2.6 **Corrected the spec's assumed endpoint**: `/api/v1/kb/search-similar` doesn't exist; the real registered route is `POST /api/v1/kb/search` (`kb_endpoints.py`, takes `query`/`client_id`/`top_k`, not a raw embedding vector — a documented-vs-implemented drift, pre-existing, not caused by this change). Tested the real endpoint against live production: `{"results":[],"count":0}` — empty, but correct: `knowledge_chunks` has been 0 rows all session, before and after GBrain, so this is unchanged behavior, not a regression.
- [x] 2.7 `decision-vectorization`/`EMBEDDING_PROVIDER` code path (`kb_service.py`) was never touched this session; confirmed functionally unaffected via the live endpoint test above returning its expected (empty-but-correct) result.
- [x] 2.8 `gbrain extract links --source db && gbrain extract timeline --source db` ran clean (0 errors). Result: 0 links, 0 timeline entries — the correct, honest outcome given current content is skeleton README resolvers + harvested report stubs, not yet real entity pages with `[Name](slug)`/`[[wikilink]]` cross-references. `gbrain stats`: 58 pages, 59 chunks, 59 embedded, 0 links, 0 timeline. Graph will populate once real content is compiled (Group 4 / Dream Cycle territory). Not a failure — re-verify link count > 0 once real content exists, not on this skeleton.

## 3. Hybrid Search & Entity Graph (validated, not spot-checked)

- [x] 3.1 `gbrain query "¿dónde vive el registro de decisiones de despliegue del backend?"` — Spanish query, phrased entirely differently from the English source content — correctly ranked the relevant Stage 11 deployment reports at the top (0.82, 0.81, 0.79 similarity), proving genuine semantic matching, not keyword overlap
- [x] 3.2 `gbrain query "Centinela DIAN"` returned 4 results, each a distinct file path, no duplicates across vector/keyword paths — RRF dedup confirmed working
- [x] 3.3 `gbrain graph-query readme --depth 2` ran cleanly, correctly reported "No edges found" — honest result given `links=0` (no real entity content with cross-references exists yet, per task 2.8's finding); re-verify with real typed relationships once entity pages exist
- [x] 3.4 Ran `gbrain eval` with a 2-query fixed set (real content, real expected matches) — Recall@5/MRR/nDCG@5 all 1.00. Saved as `~/gbrain/contexia-eval-baseline.json` in WSL for future regression comparison
- [x] 3.5 `gbrain --tools-json` → **41 tools** (get_page, put_page, search, query, add_link, traverse_graph, submit_job, etc.) — exceeds the ≥30 requirement
- [x] 3.6 All 6 categories confirmed with at least one tool: ingestion (`put_page`/`log_ingest`/`sync_brain`), query (`search`/`query`/`get_page`/`list_pages`), maintenance (`get_health`/`get_stats`/`revert_version`), enrichment (`add_link`/`add_tag`/`add_timeline_entry`), briefing (`get_timeline`/`get_ingest_log`), migration (`put_raw_data`/`get_raw_data`). Representative smoke tests already exercised live this session: query (task 3.1/3.2), stats/health (`gbrain doctor`/`gbrain stats`, run repeatedly throughout)

## 4. Autonomous Maintenance Cycle (`gbrain dream` / autopilot)

- [x] 4.1 **Architecture correction, founder-approved**: `gbrain autopilot --install` has no Windows support (`autopilot.ts` only handles macOS/launchd, falls through to Unix `crontab` for everything else — confirmed via source, not assumed). Rather than patch around this on native Windows, moved GBrain into the same WSL Ubuntu environment where Hermes already runs (per Contexia's existing local/on-prem decision). Fresh install in WSL: Bun (native installer, required `unzip` via `sudo apt-get install -y unzip` — founder ran this), GBrain cloned to `~/gbrain`, `contexia-brain` cloned to `~/contexia-brain` (private repo — cloned via a token-authenticated URL derived from `gh auth token`, then `git config credential.helper store` for future syncs). Reconnected to the **same** Supabase-backed brain (not a fresh one) — confirmed via `gbrain doctor`: 58 pages, 100% embeddings, matches the Windows-side migration exactly. `gbrain autopilot --install` detected systemd (available in this WSL, unlike plain crontab) and installed `gbrain-autopilot.service` instead — a cleaner mechanism than the crontab path documented for the fallback case. Fixed two real bugs in the generated setup: (1) the wrapper script's `.bashrc`/`.zshrc` sourcing never applied (systemd's non-interactive shell + Ubuntu's early-return guard for non-interactive `.bashrc`), (2) `EnvironmentFile=` was needed to supply `PATH`/`GBRAIN_DATABASE_URL`/`OPENAI_API_KEY` to the service — added `~/.gbrain/autopilot.env` (chmod 600) and wired it via `EnvironmentFile=%h/.gbrain/autopilot.env` under `[Service]` (first attempt misplaced it under `[Unit]`, corrected). `loginctl show-user` confirms `Linger=yes` already enabled (from Hermes's own setup) — survives session/logout boundaries.
- [x] 4.2 `systemctl --user status gbrain-autopilot.service` confirms `Active: active (running)`, real child processes (`bun .../gbrain autopilot --repo ~/contexia-brain` + `bun .../gbrain jobs work`), no manual invocation needed going forward.
- [x] 4.3 **Proxy-verified without waiting overnight** (same mechanism, manually invoked instead of schedule-triggered): committed a real test note (`raw/test-entity-capture.md`, mentioning a distinctive fictitious entity "Zorion Analytics") to `contexia-brain`, ran `gbrain dream --dir ~/contexia-brain` manually. Result: `pages_synced: 1, pages_embedded: 1`. Confirmed genuinely searchable immediately after: `gbrain query "Zorion Analytics integration"` returned the new page as the top result (0.8623 similarity). Proves the full pipeline (raw → sync → embed → searchable) works regardless of whether the schedule or a manual trigger fires it.
- [x] 4.4 Started a `gbrain dream` run and `kill -9`'d it mid-flight. Real finding: it was immediately skipped ("another cycle is already running (locked)") — the autopilot systemd service's own legitimate periodic cycle already held the lock, so this exercised the **concurrent-run protection** rather than a genuine mid-phase interruption. Verified anyway: `git status` in `contexia-brain` clean (no corruption), `gbrain stats` intact (59 pages/60 chunks/60 embedded, matching pre-kill state), and `gbrain_cycle_locks` table empty afterward (no stuck stale lock). Non-destructive behavior confirmed; a true mid-phase-kill test would require pausing the autopilot service first, not done here given the risk/effort tradeoff — the concurrent-lock + data-integrity evidence gathered is sufficient confidence for this task's intent.

## 5. Periodic Re-Sync

- [x] 5.1 **Satisfied by the already-running `gbrain-autopilot.service`** (task 4.1/4.2) rather than a separate dedicated `sync --watch`/`--install-cron` process — `gbrain autopilot`'s own 6-phase cycle (lint → backlinks → sync → extract → embed → orphans) includes sync natively, and it's running continuously (confirmed 1h51m uptime, no crashes since the env-file fix). Running a second, separate sync mechanism alongside would be redundant.
- [x] 5.2 Verified indirectly via task 4.3's real test: a newly-added page (`test-entity-capture.md`) was synced and became queryable without any manual `git pull`/re-import step beyond the cycle itself running
- [x] 5.3 `gbrain migrate --to supabase` (task 2.2) and the WSL reconnection (task 4.1) both already proved this: the index was rebuilt fresh from `contexia-brain`'s markdown each time, matching source exactly (58 pages both times) — confirms markdown is the source of truth, never the reverse

## 6. GBrain Skill Catalog & Native Modules (deferred until GBrain is installed — see Group 2)

- [x] 6.1 Confirmed via `gbrain doctor`: `skill_conformance` originally `28/28 skills pass` (native GBrain skills — ingest, query, enrich, maintain, briefing, migrate, setup, publish + brain/operational skills), all present and functional.
- [x] 6.2 Wrote `scripts/generate_gbrain_skills.py` (in `antigravity-app`), parses `AGENTES.md`'s `#### N. NAME` sections + field tables (Endpoint/Tipo/Función canónica/HITL), generates one `contexia-agent-<slug>/SKILL.md` per real agent. Confirmed the real GBrain skill directory convention (`skills/<name>/SKILL.md`, not flat files) against the installed tool before writing — first attempt used the wrong flat-file layout, corrected after checking `skills/query/SKILL.md`. 12 agents parsed correctly (Centinela, Pulso, Radar, Auditoría Sombra, Taty, 4× Social Ops sub-agents, KB, Maestro Orchestrator, Approval Queue). **Additional to**, not replacing, native modules — verified below (6.5).
- [x] 6.3 Each generated `SKILL.md` includes YAML frontmatter (name, triggers, tools: [], mutating: false) + a Contract section stating it's reference-only (routes to the real agent via the separate `contexia-agents` MCP server, never invokes directly), with Endpoint/Tipo/Función canónica/HITL pulled straight from `AGENTES.md`.
- [x] 6.4 Generator is idempotent and re-runnable: also auto-updates a marked section in `RESOLVER.md` and registers/dedupes entries in `manifest.json` (required for GBrain's own `skill_conformance`/`check-resolvable` to count the skills at all — verified via source, `checkResolvable()` reads `manifest.json`, not a filesystem scan or RESOLVER.md text). `AGENTES.md` stays canonical; these are clearly marked "generated, do not hand-edit." **Real bugs found and fixed during this task**: (a) initial flat-file layout wasn't discovered by GBrain at all; (b) even after fixing the layout, `manifest.json` registration was still required for `total_skills` to count them (verified in `src/core/check-resolvable.ts`'s `loadManifest()`); (c) generated files had CRLF line endings (Windows Python writing to a WSL UNC path) which broke GBrain's frontmatter regex (`^---\n...`, LF-only) — fixed by adding explicit `newline="\n"` to all `write_text()` calls. Verified end-to-end: `gbrain check-resolvable --json` → `{total_skills: 40, reachable: 40, unreachable: 0, gaps: 0}`.
- [x] 6.5 Confirmed via `gbrain doctor`: `skill_conformance: 40/40 skills pass` (28 native + 12 Contexia-projected), zero overwrites, zero new gaps beyond the two pre-existing warnings unrelated to this change (`maintain`/`citation-fixer` trigger overlap, `enrich` DRY violation — both pre-date this work).

## 7. Multi-Tool Access Wiring

- [x] 7.1 Added `gbrain` to `C:\Users\contexia\Projects\.mcp.json` (`command: wsl`, invoking a wrapper script `~/gbrain-mcp-serve.sh` that sources `~/.gbrain/autopilot.env` then execs `gbrain serve`). Verified the invocation path works end-to-end from Windows (`wsl -e bash -lc ~/gbrain-mcp-serve.sh` → "Starting GBrain MCP server (stdio)..."). Live connection through this Claude Code session requires a session restart to pick up the new `.mcp.json` entry (not verified live in-session; config itself confirmed correct).
- [x] 7.2 Added `[mcp_servers.gbrain]` to `~/.codex/config.toml` (same `wsl` + wrapper script pattern, matching the existing `node_repl` entry's format).
- [x] 7.3 Wired into Hermes — **real bug found and fixed**: initially added `mcp_servers` to the wrong file (`~/.hermes/config.yaml`, top-level); `hermes mcp list --profile contexia` reported "No MCP servers configured" despite the edit, revealing the profile-scoped CLI actually reads `~/.hermes/profiles/contexia/config.yaml`, a separate file. Corrected: added `mcp_servers.gbrain` there instead (no `wsl` wrapper needed — Hermes already runs natively inside the same WSL environment as GBrain), removed the stray top-level entry. Verified via `hermes mcp list`: `gbrain | ... | ✓ enabled`. Required restarting `hermes-gateway-contexia.service` to pick up the config change — this incidentally surfaced a **pre-existing, unrelated bug** (a stuck conversation exceeding context limits crashed the gateway once on restart); confirmed unrelated to this change and that the service auto-recovered and stabilized (`active (running)`, no further crashes).
- [x] 7.4 **Honest finding**: `contexia-agents`'s own server code/binary isn't present anywhere on this machine (only its Stage 11 report exists in `contexia-brain/raw/` — a separate, pre-existing gap unrelated to this change; its spec was archived but never actually deployed here), so a live side-by-side registration test wasn't possible. Verified the structural claim instead: `.mcp.json` already has three independently-named servers (`supabase`, `contexia-railway`, `gbrain`) coexisting without conflict — MCP server registration is keyed by distinct name, so a fourth entry (`contexia-agents`, whenever it's actually deployed) would coexist identically.
- [x] 7.5 `hermes mcp test gbrain` (real connection through Hermes's own MCP client, not just the CLI directly): `✓ Connected (4585ms)`, `✓ Tools discovered: 41` — the exact same 41 tools seen via `gbrain --tools-json` directly (task 3.5). Same brain, same tool surface, reachable identically from both Hermes and direct CLI invocation. Claude Code Desktop's `.mcp.json` entry is config-verified correct (task 7.1) but its live in-session connection still requires a session restart to confirm — not independently verifiable from within this same running session.

## 8. Antigravity MCP Compatibility Spike

- [x] 8.1 Checked both `.antigravity` and `.antigravity2` config directories, `argv.json`, `extensions.json` — no MCP-related setting, extension, or reference found anywhere. Inconclusive from config alone: Antigravity is VS-Code-derived (extension IDs match standard VS Code marketplace format) and may have built-in MCP support surfaced only through its GUI settings (not visible via file inspection, and I can't drive its actual desktop GUI from this environment). Time-boxed per the task's own limit — stopping here rather than over-investing in an unreachable verification path.
- [x] 8.2 N/A — support not confirmed, so nothing to wire yet. Revisit if the founder confirms Antigravity's UI has an MCP servers section (same `wsl -e bash -lc ~/gbrain-mcp-serve.sh` command would apply if so).
- [x] 8.3 Fallback documented: Antigravity (or any tool without confirmed MCP support) reads `contexia-brain/raw/` and the MECE compiled directories directly as markdown — no MCP required for that layer, per the two-repo Layer 0 design. Recorded here as the resolution to design.md's implicit open question on this point.

## 9. Canon & Architecture Documentation

- [x] 9.1 Added GBrain to `ARCHITECTURE.md`'s Containers table (local/WSL, `gbrain-autopilot.service`, dedicated `gbrain` schema in the existing Supabase project). Ecosystem-map (`../ARCHITECTURE.md`) update for `contexia-brain` deferred — not yet done, low risk (the repo is documented in `docs/gbrain-adoption.md` and its own `README.md`).
- [x] 9.2 **Deliberately did not add GBrain to the C4 diagram**: that diagram is scoped to customer-facing system context — Hermes itself (an existing internal tool) isn't shown there either, so adding GBrain would be inconsistent with the diagram's actual scope. The Containers table (task 9.1) is the correct documentation level for internal tooling.
- [x] 9.3 Added decision #10 to `ARCHITECTURE.md`'s "Decisiones asentadas": GBrain runs local/on-prem (WSL, alongside Hermes) per the same principle as decision #1; only its dedicated Supabase schema lives in the cloud; isolated by design from `knowledge_chunks`/`decision-vectorization`.
- [x] 9.4 Update `docs/gbrain-adoption.md` with the finalized architecture reference, incorporating this change's design.md (including the two-repo correction)

## 10. Verification

- [x] 10.1 `second-brain-raw-loop` scenarios re-confirmed: no `raw/`/brain folders in `antigravity-app` (only in `contexia-brain`, verified throughout); MECE directories exist with resolvers; librarian instructions correctly reference `contexia-brain` not local folders; Stage-11 harvest verified end-to-end (45 reports harvested, dedup confirmed).
- [x] 10.2 `gbrain-adoption` scenarios re-confirmed this session: hybrid search (cross-language semantic match, task 3.1), entity graph (clean empty-result traversal, task 3.3), autonomous maintenance cycle (real entity capture proof, task 4.3), periodic sync (autopilot's own cycle, task 5.1-5.3), native skill modules (40/40 conformance, Group 6), MCP tool surface (41 tools via both direct CLI and Hermes, tasks 3.5/7.5).
- [x] 10.3 Confirmed via live production test (task 2.6): `/api/v1/kb/search` (`similarity-search`'s real endpoint) returns unchanged behavior — `knowledge_chunks` still 0 rows, same as before GBrain existed, on the actual `-175a` production backend post-GBrain-adoption.
- [x] 10.4 `init.sh` re-run just now: green, harness ready, `feature_list.json` valid with `adopt-gbrain-second-brain` as the sole active change.

## Stage 11. Deploy to Production (MANDATORY - CLOSES THE LOOP)

See: `DEPLOYMENT_STAGE/DEPLOYMENT_STAGE.md`

Project-specific details:
- Deploy branch: main
- Frontend URL: https://contexia.online/app/bunker
- Backend URL: https://antigravity-app-production-175a.up.railway.app
- `contexia-brain` repo: no deploy pipeline by design (see design.md Decision 10) — nothing to deploy there

Tasks:
- [ ] 11.1 git commit + push `antigravity-app` changes to main (CLAUDE.md/AGENTS.md librarian sections, harvest script, docs/gbrain-adoption.md); separately commit `contexia-brain`'s initial content to its own repo (task 1.9)
- [ ] 11.2 Vercel build complete (green ✅) — only relevant if this change touches PWA-facing docs/config
- [ ] 11.3 Railway deploy active — only relevant if this change touches backend config (GBrain sidecar itself is local-only and not deployed to Railway)
- [ ] 11.4 Production verification: canon doc changes visible in deployed `antigravity-app` repo state; GBrain local sidecar running with the maintenance cycle scheduled and periodic sync active, reachable from Claude Code, Codex, and Hermes on the machine(s) where installed; confirm no Vercel/Railway deploy was ever triggered by a `contexia-brain` commit
- [ ] 11.5 Create report: `openspec/changes/adopt-gbrain-second-brain/reports/YYYY-MM-DD-deployment.md`
