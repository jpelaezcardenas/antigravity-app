# Archive — superseded session/phase reports

These files were moved here on **2026-06-30** from the repo root during the
"living architecture + harness" cleanup. They are **historical** (DAY*/PHASE*/
SESSION*/STAGE*/DEPLOYMENT logs, one-off setup guides, status snapshots) and are
**not canonical**. Nothing was deleted — everything is reachable here and in git
history (`git log --follow`).

**Do not treat these as current truth.** The canonical, living sources are:

- `ARCHITECTURE.md` (product) / `../ARCHITECTURE.md` (workspace) — architecture.
- `HARNESS.md` — how agents work here.
- `.antigravity/GROUND_TRUTH.md` — identity/legal.
- `AGENTES.md` — agent roster.
- `openspec/changes/<active>/` — what we build now.

If something here is still needed, promote the specific fact into the right
canonical doc rather than reviving the whole report.
